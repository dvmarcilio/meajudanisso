%h1 Atualizar Perfil
- resource_name.to_s.humanize
.row
  .span6.offset3
    = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f|
      = devise_error_messages! 
      = f.label :nome 
      = f.text_field :name
      = f.label :email
      = f.email_field :email
      = f.label  :senha
      (Deixe em branco se você não quiser alterar)
      = f.password_field :password, :autocomplete => "off"
      = f.label :confirmar_senha 
      = f.password_field :password_confirmation
      = f.label :senha_atual
      (Precisamos da sua senha atual para confirmar suas alterações)
      = f.password_field :current_password
      = f.submit "Atualizar"
= link_to "Cancelar minha conta", registration_path(resource_name), :data => { :confirm => "Tem Certeza?" }, :method => :delete
%br
= link_to "Voltar", :back
